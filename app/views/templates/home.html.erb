
<div class="main-container">
      <div class="image-row">
        <img class="avatar-image" src="/Headshot_rrd.JPG" alt="Rodrigo Rivero headshot"> 
      </div>

      <div class="name-row">
        <strong>Rodrigo Rivero Dávila

        </strong>
      </div>

<div class="links-row">
  <% @list_of_items.each do |the_item| %>
    <div class="item">
      <img src="<%= the_item.thumbnail_url %>" class="thumbnail">

      <a class="link-text" target="_blank" href="<%= the_item.link_url %>">
        <%= the_item.link_description %>
      </a>
    </div>
  <% end %>
</div>
